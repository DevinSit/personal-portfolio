{
    "private": true,
    "name": "personal-portfolio",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "start": "npm run -s dev",
        "build": "preact build --template src/index.html --no-esm --preload",
        "serve": "sirv build --cors --single",
        "dev": "preact watch --template src/index.html --port 3000",
        "lint": "eslint src"
    },
    "devDependencies": {
        "babel-eslint": "^10.0.3",
        "eslint": "^4.5.0",
        "eslint-plugin-react": "^7.18.3",
        "eslint-plugin-react-hooks": "^2.4.0",
        "node-sass": "^4.13.1",
        "preact-cli": "^3.0.0-rc.9",
        "sass-loader": "^7.3.1",
        "sirv-cli": "^0.4.5"
    },
    "dependencies": {
        "classnames": "^2.2.6",
        "preact": "^10.3.1",
        "preact-render-to-string": "^5.1.4"
    }
}
